Write a function fizzbuzz(n) that prints numbers from 1 to n.

def fizzbuzz(n):
   count = 1
   while(count < n):
      if(count % 3 == 0):
         print("Fizz")
      else if(count % 5 == 0):
         print("Buzz")
      elif(count % 3 == 0 and count % 5 == 0):
         print("FizzBuzz")
      else:
         print(count)
      count += 1
   return

Write a function find_max(lst) that manually returns the largest number in a list (without using max()).

def find_max(lst):
    max = 0
    for x in lst:
       if(x > max):
          max = x
    return max

Write a function sum_evens(lst) that adds up only the even numbers in a list and returns the total.

def sum_evens(lst):
    total = 0
    for x in lst:
       if(x % 2 == 0)
          total += x
    return total

Write a function is_palindrome(word) that returns True if a word reads the same forward and backward.

def is_palindrome(word):
    i = lens(word) - 1
    j = 0
    wordcpy = []
    while(i > -1):
       wordcpy[j] = word[i]
       i -= 1
       j += 1
    j = 0
    while(j < lens(word) - 1):
       if(wordcpy[j] == word[j]):
          continue
       if(wordcpy[j] != word[j):
          return False
       j += 1
    return True

    
