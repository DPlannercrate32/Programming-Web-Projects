Write a function count_negatives(lst) that takes a list of numbers and returns how many are negative.

def count_negatives(lst):
   count = 0
   for x in lst:
      if(x < 0):
         count += 1
   return count

Write a function cumulative_sum(lst) that returns a new list where each element is the sum of all previous elements plus itself.

def cumulative_sum(lst):
   i = 0
   lst2 = []
   while(i < len(lst) - 2):
      if(i == 0):
         lst2.append(lst[i])
      lst2.append(lst[i] + lst[i + 1])
      i += 1
   return lst2

Write a function second_largest(lst) that returns the second largest number in a list.

def second_largest(lst):
    max = 0
    max2 = 0
    for x in lst:
       if(x > max):
          max = x
       elif(max > x and x > max2):
          max2 = x
    return max2

Write a function reverse_words(sentence) that reverses each word in a sentence but keeps the word order.

def reverse_words(sentence):
    str1 = ""
    i = 0
    while(i < len(sentence) - 1):
        str1.append(sentence[-i])
        i += 1
    return str1

Write a function merge_sorted(lst1, lst2) that merges two sorted lists into a new sorted list manually (don’t use sort() or + tricks).

def merge_sorted(lst1, lst2):
    ordered = []
    lst1.append(lst2)
    count = len(lst1)
    i = 0 
    while(i < len(lst1)):
       for x in lst1:
          if(x not in ordered):
             for y in lst1:
                if(x < y and y not in ordered):
                   lesser = true
                else:
                   lesser = false
           if(lesser == true):
              ordered.append(x)
              i += 1
            
    
