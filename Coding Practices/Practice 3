Write a function sum_digits(n) that takes an integer n and returns the sum of its digits.

def sum_digits(n):
   n = strconvert(n)
   total = 0
   for x in n:
      total =+ intconvert(x)
   return total 

Write a function count_upper(s) that counts how many uppercase letters are in a string.

def count_upper(s):
   count = 0 
   for x in s:
      if x >= 65 && x <= 90:
         count += 0
   return count

Write a function fibonacci(n) that returns a list of the first n Fibonacci numbers.

def fibonacci(n):
    lst = []
    while(n > 0):
      if((n - 1) + (n - 2) == n):
         lst.append(n - 1)
         n = n - 1
      else:
         n -= 1
   return lst

Write a function remove_duplicates(s) that returns a string with only the first occurrence of each character.

def remove_duplicates(s):
   lst = []
   for x in s:
      if(x not in lst):
         lst.append(x)
   return lst

Write a function primes_up_to(n) that returns a list of all prime numbers from 2 up to n.

def primes_p_to(n):
   lst = []
   while(n > 2):
      if(n % 3 == 0 or n % 2 == 0):
         n -= 1
         continue
      else:
         lst.append(n)
         n -= 1
   return lst
